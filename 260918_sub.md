# 260918 각 항목별 버그 소재
## 1. 게임 오브젝트와 컴포넌트 

**1.1. Tag 문자열 오타 및 휴먼 에러**

[ ] 소스 코드나 판정 로직에서 Tag 이름을 직접 문자열("Player" 등)로 입력할 때 오타가 발생하지 않았는가?

[ ] 런타임에 오브젝트를 식별할 때 대소문자나 오탈자로 인해 충돌 판정이 누락될 위험은 없는가?

**1.2. Find 계열 함수의 무분별한 사용 (성능 저하)**

[ ] 씬(Scene) 전체를 탐색하는 Find 함수가 매 프레임 호출되는 Update() 내부에 포함되어 프레임 드랍(렉)을 유발하고 있지 않은가?

**1.3. 컴포넌트 참조 누락 및 방어적 코딩 부재**

[ ] Rigidbody나 커스텀 컴포넌트가 인스펙터나 코드상에서 연결되지 않은 상태로 실행되어 NullReferenceException 크래시를 유발할 위험은 없는가?

[ ] 초기화 단계에서 컴포넌트 존재 여부를 확인하는 방어적 코드(.name 체크 등)가 누락되지는 않았는가?

**1.4. Update 함수 내 Time.deltaTime 미적용 (사양 편차 버그)**

[ ] 사양이 좋은 기기와 나쁜 기기에서 이동 속도나 게임 판정이 달라지도록 Update 내에서 시간 보정(Time.deltaTime)을 누락하지 않았는가?

**1.5. 라이프사이클(이벤트 함수) 호출 시점 혼동**

[ ] Awake(자기 자신 초기화), Start(첫 프레임 전 외부 값 연동), OnEnable(활성화 시점마다 반복 호출)의 역할을 혼동하여 로직을 잘못된 함수에 배치하지 않았는가?

**1.6. Playmode 상태에서의 작업 손실**

[ ] 게임 실행 중(Playmode)인 상태에서 인스펙터 값을 수정하거나 오브젝트를 조작한 후, 이를 끄지 않아 작업한 내용이 전부 초기화되는 실수를 범하지 않았는가?

## 2. Input Manager, Scene, Transform
**2.1. 빌드 세팅(Build Settings) 미등록에 따른 Scene 로드 실패**

[ ] 전환하려는 씬(Scene)이 File/Build Setting에 정상적으로 등록되어 있는가? (등록되지 않은 상태에서 이름이나 인덱스로 로드 시도 시 에러 발생 유발)

**2.2. DontDestroyOnLoad 객체의 중복 생성(메모리 누수 및 복제) 버그**

[ ] 씬이 전환되거나 리로드될 때 파괴되지 않는 오브젝트(DontDestroyOnLoad)가 조건 검사(FindObjectOfType 등을 통한 중복 체크) 누락으로 인해 무한히 복제되고 있는가?

**2.3. 단위 벡터(Normalization) 누락으로 인한 대각선 이동 속도 왜곡**

[ ] 키 입력(Horizontal, Vertical)을 받아 벡터를 생성할 때, 대각선 이동 시 속도가 빨라지는 현상을 막기 위해 정규화(Normalize 또는 normalized) 처리를 누락하지 않았는가?

**2.4. Euler 각(3축 회전) 사용 시 짐벌락(Gimbal Lock) 및 보간 오류**

[ ] 오브젝트 회전 시 단순 Euler 각을 사용하여 짐벌락 현상이나 보간 시 예상치 못한 회전 오류를 유발하고 있지 않은가? (쿼터니언 Quaternion.Slerp 등 사용 확인)

**2.5. 조건문 작성 실수로 인한 입력 판정 오작동**

[ ] 이동/회전 방향 검사 시 전체 벡터가 아닌 특정 축(movement.z == 0 등)만 비교하여, 특정 방향 키를 눌렀을 때 로직이 씹히거나 비정상적으로 차단되는 실수를 범하지 않았는가?

## 3. prefab, 생성과 파괴, audio
**3.1. 프리팹(Prefab) 설계도와 인스턴스(객체)의 혼동에 따른 파괴 오류**

[ ] 파괴(Destroy)를 수행할 때 실제 생성된 게임 오브젝트가 아니라 프리팹 원본 설계도 변수를 파괴하려고 시도하여 객체가 사라지지 않거나 에러를 유발하고 있지 않은가?

[ ] 리스트나 배열을 통해 관리 중인 인스턴스 객체를 정확히 지정하여 파괴하고 있는가?

**3.2. 컬렉션 범위를 벗어난 인덱스 접근 (Index Out of Range)**

[ ] 배열이나 리스트로 관리되는 오브젝트들을 순회하거나 파괴하는 과정에서, 배열의 크기를 벗어나는 인덱스 접근으로 인해 예외(ArgumentOutOfRangeException)가 발생할 위험은 없는가? (범위 체크 return 방어 코드 유무 확인)

**3.3. Instantiate 시 부모-자식 변환(Transform) 누락 및 위치 어긋남**

[ ] 탄환 등을 생성할 때 Instantiate(_bulletPrefab, transform)처럼 부모를 잘못 지정하거나, 생성된 총알의 위치(position)와 회전(rotation)을 총구(muzzlePoint) 위치로 올바르게 동기화해주지 않아 엉뚱한 곳에서 발사되는 버그가 있는가?

**3.4. 오디오 소스(Audio Source)와 리스너(Listener) 누락 경고**

[ ] 사운드를 출력하는 오브젝트에 AudioSource 컴포넌트가 누락되었거나, 씬 내에 필수 컴포넌트인 AudioListener가 없어 사운드 재생 시 경고나 무음 버그가 발생할 상황은 없는가?

**3.5. 빈번한 힙(Heap) 메모리 할당 및 파괴로 인한 프레임 드랍(GC 스파이크)**

[ ] 탄환 생성과 파괴(Instantiate, Destroy)를 무분별하게 반복하여 가비지 컬렉터(GC) 부하로 인한 프레임 튐 현상(렉)을 유발할 구조로 코드가 짜여 있지는 않은가? (오브젝트 풀링 등 최적화 고려 필요)

## 4. 물리 및 충돌, Raycast
**4.1. Rigidbody 이동 시 불필요한 Time.deltaTime 곱셈으로 인한 속도 저하 버그**

[ ] Transform 이동과 혼동하여 _rigidbody.velocity나 AddForce를 적용할 때 Time.deltaTime을 곱해버려, 물리 엔진 갱신 주기와 충돌해 오브젝트의 움직임이 극단적으로 느려지거나 멈추는 현상이 발생하고 있지 않은가?

**4.2. FixedUpdate 내의 키 입력 누락(씹힘) 현상**

[ ] 물리 처리라는 이유로 Update() 대신 FixedUpdate() 내부에서 Input.GetKeyDown 같은 키 입력을 받아, 프레임 주기에 따라 입력이 스킵되거나 키 씹힘 현상이 발생할 구조로 코드가 짜여 있지는 않은가? (키 입력은 Update, 물리 연산은 FixedUpdate로 분리 확인)

**4.3. 높은 위치에서 낙하 시 충돌체 통과 버그 (터널링 현상)**

[ ] Rigidbody를 가진 물체를 너무 높은 곳에서 떨어뜨리거나 속도가 지나치게 빠를 때, 물리 엔진이 프레임 간격을 건너뛰어 바닥이나 벽을 그대로 통과해 버리는 버그가 발생할 위험은 없는가? (Collision Detection 모드 변경 검토 필요)

**4.4. RaycastAll 배열 순회 시 Null 또는 예외 발생 위험**

[ ] Physics.RaycastAll을 통해 얻은 RaycastHit[] 배열을 사용할 때, 결과 배열의 길이(Length)를 검사하지 않거나 빈 배열에 접근하여 예외를 유발할 위험은 없는가? (또는 반환된 hit.transform에 유효한 컴포넌트가 있는지 방어 코드가 있는가?)

**4.5. 타깃 미지정 상태의 마우스 우클릭 이동에 따른 NullReference 예외**

[ ] 마우스 좌클릭으로 유닛을 선택하지 않았거나(_target == null), 빈 바닥(Ground)을 클릭해 타깃이 해제된 상태에서 우클릭으로 이동 함수(SetDestination)를 호출하여 NullReferenceException 에러가 발생하고 있지는 않은가? (if (_target == null) return; 방어 코드 유무 확인)

## 5. Layermask
**5.1. LayerMask 직접 동등 비교(==)로 인한 판정 누락 버그**

[ ] 레이어 마스크를 판별할 때 비트 연산(&)을 사용하지 않고 layer == TargetLayer.value와 같이 단순 == 비교를 수행하여, 다중 레이어가 선택되었거나 비트 자릿수가 일치하지 않을 때 충돌 판정이 통째로 무시되는 버그가 발생하고 있는가? ((TargetLayer.value & layer) != 0 형태로 비트 연산 확인)

**5.2. Raycast 시 LayerMask 인자 누락으로 인한 의도치 않은 오브젝트 감지**

[ ] Physics.Raycast를 호출할 때 필터링 역할을 하는 TargetLayer 인자를 누락하여, 원치 않는 주변 오브젝트나 배경(Ground 등)이 레이에 먼저 감지되어 터렛이나 유닛의 상호작용이 엉키는 버그가 발생하고 있는가?

**5.3. 비트 시프트(<<) 및 반전 연산자(~) 실수로 인한 레이어 제어 오류**

[ ] 확장 메서드를 통해 레이어를 추가(Add)하거나 제거(Remove)할 때, 1 << layer 자릿수 계산을 실수하거나 제거 로직에서 비트 반전 연산자(~)를 누락하여 원하는 레이어가 정상적으로 켜지거나 꺼지지 않는 오작동 버그가 있는가?

**5.4. 필수 컴포넌트 누락으로 인한 런타임 NullReference 예외**

[ ] 스크립트 내부에서 Rigidbody 등의 물리 컴포넌트를 가져다 쓰거나 제어할 때, 해당 컴포넌트가 게임 오브젝트에 부착되어 있지 않아 런타임 에러(NullReferenceException)를 유발할 위험은 없는가? RequireComponent(typeof(Rigidbody) 적용 여부 확인)

## 6. UI 구성
**6.1. 해상도 변경에 따른 UI 위치 이탈 및 잘림 현상 (앵커/피벗 미설정)**

[ ] 다양한 모니터 해상도나 화면 비율에 맞춰 UI가 유연하게 대응하도록 앵커(Anchor)와 피벗(Pivot)을 올바르게 설정하지 않아, 화면이 바뀔 때 UI가 화면 밖으로 밀려나거나 잘리는 레이아웃 버그가 발생하고 있는가?

**6.2. 폰트 한글 깨짐 및 아틀라스(Atlas) 누락 버그**

[ ] TextMeshPro를 사용할 때 한글을 지원하지 않는 기본 폰트를 그대로 사용하거나 필요한 폰트 자산/아틀라스 설정을 누락하여, 게임 내 텍스트가 네모 박스(깨짐 현상)나 물음표로 출력되는 버그가 발생할 위험은 없는가?

**6.3. 이벤트 리스너 중복 등록 및 메모리 누수 버그**

[ ] 버튼 클릭 이벤트 등을 등록할 때 OnEnable()에서 AddListener를 사용하면서 대응하는 OnDisable()에서의 RemoveListener 해제 처리를 누락하여, 오브젝트가 활성화될 때마다 이벤트가 중복 호출되거나 메모리 누수(Memory Leak)가 발생하고 있는가?

**6.4. 초기화되지 않은 컴포넌트 참조로 인한 NullReference 예외**

[ ] PlayerUIController에서 GetComponentInChildren<PlayerWeapon>()을 통해 무기 컴포넌트를 캐싱할 때, 대상 오브젝트에 해당 컴포넌트가 존재하지 않거나 타이밍 문제로 _weapon이 null인 상태가 되어 Update()문 내의 RefreshMegazineUI()에서 NullReferenceException 에러가 터지고 있지는 않은가?

## 7. Static, 싱글톤, 오브젝트 풀
**7.1. 싱글턴 인스턴스 초기화 타이밍 문제로 인한 NullReference 예외**

[ ] 여러 컴포넌트(Player, GameManager 등)가 각자의 Awake() 단계에서 서로의 싱글턴 인스턴스나 전역 상태를 참조할 때, 유니티의 스크립트 실행 순서(Execution Order) 보장 부재로 인해 아직 SetSingleton()이 호출되지 않아 인스턴스가 null인 상태로 접근하여 NullReferenceException이 발생하는가?

**7.2. 씬 전환 시 싱글턴 중복 생성 및 씬 파괴 오류**

[ ] 싱글턴 클래스에 DontDestroyOnLoad(gameObject)를 적용했으나, 씬을 다시 로드하거나 되돌아왔을 때 기존 오브젝트가 파괴되지 않고 누적되거나 중복 생성되어 매니저가 여러 개 공존하는 오작동 버그가 있는가? (이미 존재하는 인스턴스가 있을 경우 현재 생성된 자신을 Destroy(gameObject) 처리하는 로직 검증 필요)

**7.3. 싱글턴 프로퍼티의 자동 검색(FindObjectOfType) 남용으로 인한 성능 저하 및 엉뚱한 객체 참조**

[ ] Instance 프로퍼티의 get 접근자 내부에서 _instance == null일 때 동적으로 FindObjectOfType<T>()를 호출하도록 구현했을 경우, 씬 내에 해당 매니저가 존재하지 않거나 비활성화되어 있으면 매 프레임 혹은 호출 때마다 무거운 검색 연산이 수행되어 프레임 드랍(스파이크)이 발생하거나 의도하지 않은 오브젝트를 가져오는가?

**7.4. 오브젝트 풀 Take() 시 풀 고갈(IsEmpty)에 대한 예외 처리 누락**

[ ] 오브젝트 풀의 용량(Size)을 초과하여 아이템을 계속 꺼내가려 할 때, Take() 메서드 내부에서 IsEmpty 검사를 누락하거나 반환값이 null일 때의 방어 코드가 없어서 _pool[Count] 배열 인덱스 초과(IndexOutOfRangeException) 또는 null.transform 접근으로 인한 크래시가 발생하는가?

**7.5. 오브젝트 풀 반환(Return) 누락으로 인한 풀 고갈 및 메모리 누수**

[ ] 총알이나 이펙트 등의 오브젝트가 사용 완료된 뒤 Destroy 대신 정해진 풀의 ReturnToPool()을 호출하여 상태를 되돌리고 비활성화(SetActive(false))해야 하는데, 이를 누락하여 풀 내의 가용 객체가 바닥나고 더 이상 오브젝트를 생성·재사용할 수 없게 되는 버그가 있는가?

## 8. Delegate, 이벤트, Unity Action, Unity Events
**8.1. 델리게이트 구독 해제 누락으로 인한 메모리 누수(Memory Leak) 및 댕글링 참조**

[ ] UI나 바인더(PlayerUIBinder 등)가 활성화/비활성화되거나 파괴될 때(OnDisable 또는 OnDestroy), _player.OnHealthChanged -= ...와 같은 구독 해제 코드를 누락하여 이미 파괴된 오브젝트의 메서드를 메모리가 계속 참조하게 되는 메모리 누수 및 예외 발생 가능성이 있는가?

**8.2. 대입 연산자(=) 사용으로 인한 기존 구독 목록 소실(덮어쓰기)**

[ ] 델리게이트나 이벤트를 등록할 때 누락 방지를 위해 누적 연산자인 +=를 써야 함에도 불구하고, 실수로 대입 연산자 =를 사용하여(Player.OnHealthChanged = RefreshHealthUI;) 기존에 등록되어 있던 다른 UI나 기능들의 구독이 전부 지워지고 마지막에 등록된 기능만 동작하는 오작동 버그가 발생하는가?

**8.3. 이벤트 발화 시 널(Null) 체크 누락으로 인한 NullReferenceException**

[ ] 델리게이트나 액션에 등록된 메서드가 단 하나도 없는(구독자가 없는) 상태에서 OnHealthChanged(playerHp);처럼 널 체크 없이 곧바로 호출(Invoke)하여 NullReferenceException 크래시가 발생하는가? (OnHealthChanged?.Invoke(...) 형태로 방어 코드가 작성되어 있는지 확인 필요)

**8.4. 씬 전환 또는 오브젝트 생명주기에 따른 초기화 타이밍 오류 (Null Ref)**

[ ] TempPlayer 내부의 Exp 같은 ObservableProperty<T> 커스텀 클래스가 선언만 되고 인스턴스화(new ObsevableProperty<float>(0))되지 않았거나, Awake 시점의 초기화 순서 문제로 인해 다른 스크립트가 참조하려 할 때 null 상태여서 예외가 발생하는가?

**8.5. 이벤트/액션 내부에서 무한 루프 또는 연쇄 상태 변경(Stack Overflow) 유발**

[ ] 프로퍼티의 set 접근자 내부나 델리게이트 체인으로 연결된 메서드 안에서 다시 자신의 값을 변경하는 로직이 섞여 들어가, 값이 바뀔 때마다벤트를 호출하고 그 이벤트가 다시 값을 바꾸는 무한 재귀 호출로 인해 StackOverflowException이 발생하는가?

## 9. 코루틴
**9.1. new WaitForSeconds 반복 생성으로 인한 가비지 컬렉터(GC) 스파이크 및 성능 저하**

[ ] 코루틴 내부의 while 문 등에서 매번 yield return new WaitForSeconds(1f);처럼 new 키워드를 사용하여 대기 객체를 생성할 경우, 매 주기마다 힙(Heap) 메모리에 쓰레기(Garbage)가 누적되어 가비지 컬렉터(GC)가 빈번히 동작하고 게임이 뚝뚝 끊기는 프레임 드랍(스파이크) 현상이 발생하는가? (필드에 미리 캐싱해 두거나 WaitForSeconds 객체를 재활용하고 있는지 확인 필요)

**9.2. 코루틴 참조 변수(Coroutine) 초기화 누락으로 인한 중복 실행 및 정지 불가 오류**

[ ] StartCoroutine() 실행 시 반환된 코루틴 레퍼런스를 _routine 변수에 담을 때, 코루틴이 스스로 종료되거나 외부에서 멈출 때 _routine = null; 처리를 누락하여 이미 끝난 코루틴임에도 _routine != null 상태로 남아버려 새로운 코루틴이 실행되지 않거나 엉뚱한 코루틴을 StopCoroutine() 하려다 예외/오작동이 발생하는가?

**9.3. 파괴된(Destroyed) GameObject에서의 코루틴 실행 및 컴포넌트 생명주기 불일치**

[ ] 오브젝트가 비활성화되거나 파괴(Destroy)되는 시점에도 백그라운드에서 돌고 있던 코루틴이 정지되지 않고 계속 실행되려 하거나, 이미 파괴된 컴포넌트의 변수를 참조하려다 MissingReferenceException 크래시가 발생하는가?

**9.4. StopCoroutine() 사용 시 문자열(String) 방식 남용으로 인한 중지 누락**

[ ] 코루틴을 멈출 때 StopCoroutine("MyRoutine")과 같이 메서드 이름을 문자열로 전달할 경우, 오타가 발생해도 컴파일 에러가 나지 않아 런타임에 코루틴이 멈추지 않고 계속 폭주하는 버그가 발생하는가? (반환된 Coroutine 객체 레퍼런스나 IEnumerator 변수를 직접 넘겨서 제어하는 것이 안전)

**9.5. Time.timeScale = 0 (게임 일시정지) 상태에서의 코루틴 무한 대기 버그**

[ ] 게임을 일시정지하기 위해 Time.timeScale = 0으로 설정했을 때, 일반적인 WaitForSeconds는 시간 흐름까지 멈춰버려 일시정지 해제 조건 등을 체크하는 코루틴 전체가 영구적으로 멈추거나 씹히는 오작동이 발생하는가? (일시정지 중에도 흘러가야 하는 대기 처리에는 WaitForSecondsRealtime 사용 고려 필요)

## 10. 애니메이션
**10.1. 애니메이터 파라미터 문자열 직접 사용으로 인한 오타 및 성능 저하 (매직 스트링)**

[ ] 애니메이터 컴포넌트에 접근할 때 _animator.SetBool("IsMove", true)나 _animator.SetTrigger("Attack")처럼 파라미터 이름을 문자열 그대로 직접 전달할 경우, 코드 내 오타가 발생해도 컴파일 시점에 발견되지 않아 런타임에 애니메이션이 정상 전이되지 않는 버그가 발생하는가? (필기에서 권장하듯 Animator.StringToHash()를 이용해 해시값으로 변환하여 관리하고 있는지 확인 필요)

**10.2. 트리거(Trigger) 파라미터 중복 호출 또는 잔류로 인한 오작동**

[ ] 단발성 애니메이션 제어를 위해 사용하는 SetTrigger()를 짧은 시간에 연속으로 호출하거나, 조건이 충족되지 않아 트리거가 소모되지 않은 상태로 누적될 때 의도하지 않은 시점에 공격 등의 애니메이션이 갑자기 튀어나거나 발동하는 버그가 발생하는가?

**10.3. 애니메이션 이벤트(Animation Event) 대상 메서드 이름 불일치 및 누락**

[ ] 타임라인에 등록한 애니메이션 이벤트(예: OnHit)와 실제 스크립트에 구현된 메서드의 이름이 서로 다르거나, 스크립트가 해당 오브젝트에 부착되어 있지 않아 재생 도중 경고(Warning) 및 런타임 예외가 발생하며 이벤트가 무시되는가?

**10.4. 씬 전환 또는 오브젝트 비활성화 시 애니메이터 상태 불일치 (파라미터 초기화 누락)**

[ ] 캐릭터가 이동 중이거나 공격 모션 도중에 강제로 UI가 꺼지거나 씬이 전환되어 오브젝트가 비활성화(OnDisable)될 때, 애니메이터 내부의 bool이나 float 파라미터 상태(예: IsMove = true)가 그대로 남아있다가 나중에 다시 활성화되었을 때 가만히 서 있어도 걷는 애니메이션이 재생되는 등 상태 동기화 오류가 발생하는가?

**10.5. 루트 모션(Root Motion) 활성화로 인한 위치 충돌 및 의도치 않은 좌표 이탈**

[ ] 캐릭터 컨트롤러나 이동 스크립트에서 직접 위치(Transform 또는 Rigidbody)를 제어하고 있음에도 애니메이션 클립의 루트 모션(Apply Root Motion)이 켜져 있거나 설정이 잘못되어, 애니메이션 자체의 이동 값과 스크립트의 이동 값이 충돌하여 캐릭터가 공중으로 
