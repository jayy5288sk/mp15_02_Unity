# 260917 Unity(13) 애니메이션
## 1. 애니메이션
### 1.1. 개요
- 보통은 프로토타입을 만들 때는 가장 먼저 큐브 등의 도형으로 선행이 되지만 애니메이션이 입혀지는 것과는 차원이 다르다.
- 특수한 형태가 있는 골격에서는 어떻게 움직여야 하느냐가 우선된다.
- 보통은 인체 골격에서 출발한다. 
- 관절 하나하나에 transform 정보가 담겨있는 것이다. 
- 하지만 이 과정에서는 애니메이션을 직접 만들지 말 것을 당부.
- 애니메이션 클립, 애니메이션 컨트롤러, 애니메이터로 구동이 된다.
- 오디오 때도 소스와 클립이 있었다.
- 클립은 애니메이션 하나 단위의 파일이고 애니메이터는 소스이며 컨트롤러는 어떤 상황에서 어떤 애니메이션으로의 변환으로 사용. 

### 1.2. 사용
사용하던 Scene에서 작업을 할 것이다. 

시작했을 때, 정면에 문 하나가 있는데 문을 설치한다고 해보자.

![alt text](image-239.png)

컨트롤러가 네모

![alt text](image-240.png)

이 상태에서 복사 하나를 해서 DoorOpen이라고 해준다.

![alt text](image-241.png)

그 다음, 컨트롤러를 본다.

![alt text](image-242.png)

옮겨 붙여줄 수 있고 연결이 된다.

![alt text](image-243.png)

선택된 상태에서 애니메이션 창을 본다.

![alt text](image-244.png)

프로퍼티를 소스 코드 딴에서 조정하는 것이 좋다.

일단 이 곳에 Rotation 추가

![alt text](image-245.png)

기본값은 0인 상태이고 끝자락에 가더라도 0인 상태이다. 

끌어다준 다음

![alt text](image-246.png)

일단 하위로 있는 큐브의 위치 조정으로 한 상태면 다음과 같이 된다.

![alt text](image-247.png)

DoorIdle은 일단 시작점과 끝점을 동일하게 놓은 상태로 DoorOpen에서 작업을 하면

![alt text](image-248.png)

이러면 특정 조건을 줘서 애니메이션을 통해 UI 처리를 할 수도 있다.

좀 더 진보적인 것에 대해서의 키워드는 'DoTween'이란 패키지가 존재.

### 1.3. 조건 설정

이제 애니메이터에 Door가 걸린 것을 확인 한 후 플레이를 하면 뭔가가 실행된다는 것을 확인 가능.

![alt text](image-249.png)

이것을 make transition을 통해 open에 연결

![alt text](image-250.png)

여기서 조건을 설정해볼 것이다.

![alt text](image-251.png)

연결된 트랜지션을 선택하면 기본적으로 이런 상태인데 

![alt text](image-252.png)

조건 설정은 parameter로 확인하고 bool로 생성

![alt text](image-253.png)

![alt text](image-254.png)

인스펙터에서 트랜지션 설정창을 보면 

![alt text](image-255.png)

전이 조건 자체를 isOpen이라는 변수가 True일 때는 전환을 시켜주겠다는 뜻이 된다. 

현재 조건에서는 아직까진 어색하다.

![alt text](image-256.png)

hasExitTime을 끄면 아까보다 바로 간다.

뭔가 좀 변화를 주고 싶다면 Setting을 열면 된다. 

![alt text](image-257.png)

현재는 한 애니메이션이 반복 설정이 되고 있는데 애니메이션 파일을 클릭해서 LoopTime을 끄고 실행하면 이제 한 번만 한다. 

![alt text](image-258.png)

DoorClose를 만들도록 하자.

일단 이 bool을 받기 위해 Script를 만든다.

![alt text](image-259.png)

일단 tag 쪽으로 가자. 

![alt text](image-260.png)

알고 있어야 할 컴포넌트는 애니메이터

![alt text](image-261.png)

![alt text](image-262.png)

현 단계에서는 파라미터 이름과 값이 들어간다.

트리거는 단발성이기 때문에 SetTrigger의 경우에는 파라미터 이름만 들어간다. 

적용해보면 

![alt text](image-263.png)

문자열은 자주 쓰이는 방식이 권장하고 싶은 사항은 아님.

일단 실행.

![alt text](image-264.png)

애니메이션 클립을 가지고 구조가 어떻게 돌아갈지를 생각하면 된다. 

이 문이 변경할 수 있는 애니메이션의 수가 무한인가 생각하면 2개밖에 없어 보인다. 

캐릭터가 Idle 상태에서 움직이거나 공격이거나 그로기이거나 데미지 입는 상태로 될 수 있는 것이다.

이 유한한 상태로 존재하는 것을 상태를 전환하는 모델을 FSM, Finite State Machine이라고 한다.

상태라는 개념으로 구분해서 전이하는 조건을 설정하는 개념.

그 FSM을 도입해서 쓰는 것이 애니메이터 컨트롤러이다. 

**FSM에 대해서는 추가적인 개념에 대해 알아둘 것.**

Scene을 새로 만들어서 살펴보자.

![alt text](image-265.png)

mixamo로 들어가서 로그인

![alt text](image-266.png)

믹사모에서는 애니메이션 위주가 될 예정이다. 

Y Bot으로 애니메이션을 가지고 할 것인데 T-Pose인지 확인하고 download

![alt text](image-267.png)

idle, walk, run을 이런 형식으로 받을 것.

![alt text](image-268.png)

폴더 하나를 지정해서 다 드래그 드롭한다.

![alt text](image-269.png)

이제 아바타의 Rig를 살펴보면 Generic으로 되어 있는데 

![alt text](image-270.png)

이 제네릭은 휴머노이드 골격이 아닌 경우, 그러니까 슬라임같은 캐릭터에 대해서 적용하는 골격이다. 

휴머노이드로 설정하고 Definition 추가 설정 없이 Apply로 적용

![alt text](image-271.png)

이제 애니메이션들을 적용을 해볼 것.

![alt text](image-272.png)

![alt text](image-273.png)

미리 추가할 것은 Idle.

![alt text](image-274.png)

이상한 자세를 한다.

그래서 Idle을 적용하려면 똑같이 Humanoid로 바꿔줘야 한다.

![alt text](image-275.png)

그리고 올려준다.

![alt text](image-276.png)

현재 이런 상태인데 다 수정을 해줘야 한다.
![alt text](image-277.png)

이렇게 개별로 바꿔준다.

![alt text](image-278.png)

bool로 걷는 것과 뛰는 관점이 다를 수 있는 것이고 아니면 float값을 기준으로 할 수 있는 것이다. 경우는 다양함. 

지금은 자연스럽게 섞이도록 해볼 것이다. 기존의 애니메이션 등록했던 것을 삭제하고 loop를 걸어준 다음 파라미터를 설정.

![alt text](image-279.png)

### 1.4. 블랜드 트리

그다음 블랜드 트리 설정.

![alt text](image-280.png)

추가한 블랜드 트리를 더블 클릭

![alt text](image-281.png)

더블클릭했을 때 인스펙터에서 파라미터를 뭘 쓸지 선택할 수 있다. 

![alt text](image-282.png)

지금 블랜딩이 된다.

![alt text](image-283.png)

일단 우측 하단의 블랜드 트리를 통해 보여지고 있다.

![alt text](image-284.png)

![alt text](image-285.png)

애니메이터 컨트롤러 단에서 접근해서 MoveSpeed를 SetFloat로 접근해서 쓰면 된다. 설정하는 과정이 좀 헷갈릴 수 있다는 것. 

블랜드 트리가 특정 구간을 섞어준다는 개념이라고 생각하면 된다. 

### 1.5. 아바타 설정

현 상태에서는 스켈레톤에 맞지 않아서 발이 접질러진것처럼 움직이는데 다음과 같이 Rig에서 설정해줘야 한다.

![alt text](image-286.png)

이렇게 아바타에 맞게 설정하면 이제는 정상적으로 애니메이션이 동작함을 볼 수 있다.

![alt text](image-287.png)

### 1.6. root 관련
그런데 현재 애니메이션을 지속적으로 속도를 통해 갱신하는 경우 상당수 벗어나거나 하는 이상증상이 존재한다.

![alt text](image-288.png)

여기서 Apply Root Motion을 체크 해제하면 

![alt text](image-289.png)

이렇게 된다.

![alt text](image-290.png)

전보단 Transition이 훨씬 덜 엇나간다. 

애니메이션 옵션을 보면

![alt text](image-291.png)

루트의 영향을 받게 할 것인지에 대한 여부이다. 그래서 애니메이터 선에서 끄고 하는 편이 많다. 

개별 애니메이션에서 상세 부분을 조정을 할 수는 있다. 

단, 최소한 Bake Into Pose를 이용하면 Transform의 좌표를 기준한다는 의미처럼 설정할 것들이 많아서 헷갈릴 수 있으나 당연히 있을만한 것들이 존재한다는 것을 보는 정도로 알아둘 것. 

### 1.7. 개별 애니메이션

일단 Idle 추가

![alt text](image-292.png)

Layer라는 것이 있는데 Layermask와는 다르게 아트 쪽에서 쓰는 Layer인데 캔버스를 개별로 써서 한 번에 합치는 느낌이다.

Layer 추가

![alt text](image-293.png)

두번째 Layer에는 Run을 추가하자. 

![alt text](image-294.png)

여기서 Weight를 보면 비중을 두면 둘수록 좀 더 애니메이션이 쏠린다.

![alt text](image-295.png)

그런데 블랜드 트리와 유사하더라도 다른 것이 우선 순위를 정해서 얼마만큼의 비중으로 둘 지 설정한다.

추가할 것.

![alt text](image-296.png)

이 중 Humanoid를 볼 것.

![alt text](image-297.png)

![alt text](image-298.png)

이런식으로 설정한다.

![alt text](image-299.png)

![alt text](image-300.png)

이런식이다.

### 1.8. 이벤트

Events를 클릭

![alt text](image-301.png)

여기서 조그마한 부분을 클릭해서 끌어다가 놓는다.

![alt text](image-302.png)

Apply로 적용

![alt text](image-303.png)

이제 스크립트를 만들어 OnHit 함수를 만들면 된다. 

![alt text](image-304.png)

![alt text](image-305.png)

### 1.9. 문자열 기반 외의 방식

스크립트를 추가하여 다음과 같이 명명

![alt text](image-306.png)

```csharp
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BotController : MonoBehaviour
{
    // 1. W키를 누르면 이동 시작, 떼면 이동 종료 (로그만)
    // 2. 스페이스를 누르면 공격(로그만)

    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.W))
            MoveStart();

        if(Input.GetKeyUp(KeyCode.W))
            MoveEnd();

        if (Input.GetKeyDown(KeyCode.Space))
            Attack();
    }

    private void MoveStart()
    {
        Debug.Log("이동 시작");
    }

    private void MoveEnd()
    {
        Debug.Log("이동 종료");
    }

    private void Attack()
    {
        Debug.Log("꽁껶");
    }
}

```

![alt text](image-307.png)

이것을 잘 이용해볼 것이다. 

일단 어떤 상황에건 나가야 한다면 

![alt text](image-308.png)

이어줘야 한다. exit까지 해주면

![alt text](image-309.png)

가장 많이 쓰이는 곳에는 죽었을 때에 이어준다. 

어떤 상태에서건 이어주는 것. 

문자열에 관한 것을 말할 것인데 코드 상황은 직관적인 상황이고 기준 자체도 상황에 맞는 애니메이션이다.

그런데 아이템 쓰는 애니메이션의 경우 한두가지로 돌려쓰는 경우가 많다. 

그래서 정석은 아니지만 구성을 하는 방법은 다음과 같다. 

```csharp
using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BotController : MonoBehaviour
{
    private Animator _animator;

    public event Action<bool> OnMove;
    public event Action OnAttack;

    private void Awake()
    {
        CacheComponents();
    }
    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.W))
            MoveStart();

        if(Input.GetKeyUp(KeyCode.W))
            MoveEnd();

        if (Input.GetKeyDown(KeyCode.Space))
            Attack();
    }

    private void MoveStart()
    {
        Debug.Log("이동 시작");
        //_animator.SetBool("IsMove", true);
        OnMove?.Invoke(true);
    }

    private void MoveEnd()
    {
        Debug.Log("이동 종료");
        //_animator.SetBool("IsMove", false);
        OnMove?.Invoke(false);
    }

    private void Attack()
    {
        Debug.Log("꽁껶");
        //_animator.SetTrigger("Attack");
        OnAttack?.Invoke();
    }

    private void CacheComponents()
    {
        _animator = GetComponent<Animator>();
    }
}

```

```csharp
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BotAnimationHandler : MonoBehaviour
{
    [SerializeField] private string _moveAnimParam;
    [SerializeField] private string _attackAnimParam;

    private int _move;
    private int _attack;

    private BotController _controller;
    private Animator _animator;

    private void Awake()
    {
        CacheComponents();
        Init();
    }

    private void OnEnable()
    {
        BindBotEvents();
    }

    private void OnDisable()
    {
        UnbindEvents();
    }

    private void Init()
    {
        _move = Animator.StringToHash(_moveAnimParam);
        _attack = Animator.StringToHash(_attackAnimParam);
    }

    private void CacheComponents()
    {
        _controller = GetComponent<BotController>();
        _animator = GetComponent<Animator>();
    }

    private void BindBotEvents()
    {
        
    }

    private void UnbindEvents()
    {
        
    }
}

```

이제 바인딩을 해야 한다. 관리하는 함수를 설정. 

```csharp
private void SetMoveAnim(bool isMove)
{
    _animator.SetBool(_move, isMove);
}

private void SetAttackAnim()
{
    _animator.SetTrigger(_attack);
}
```

이벤트 걸어주고 빼주는 것 전개

```csharp
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BotAnimationHandler : MonoBehaviour
{
    [SerializeField] private string _moveAnimParam;
    [SerializeField] private string _attackAnimParam;

    private int _move;
    private int _attack;

    private BotController _controller;
    private Animator _animator;

    private void Awake()
    {
        CacheComponents();
        Init();
    }

    private void OnEnable()
    {
        BindBotEvents();
    }

    private void OnDisable()
    {
        UnbindEvents();
    }

    private void BindBotEvents()
    {
        _controller.OnMove += SetMoveAnim;
        _controller.OnAttack += SetAttackAnim;
    }

    private void UnbindEvents()
    {
        _controller.OnMove -= SetMoveAnim;
        _controller.OnAttack -= SetAttackAnim;
    }

    private void SetMoveAnim(bool isMove)
    {
        _animator.SetBool(_move, isMove);
    }

    private void SetAttackAnim()
    {
        _animator.SetTrigger(_attack);
    }
    private void Init()
    {
        _move = Animator.StringToHash(_moveAnimParam);
        _attack = Animator.StringToHash(_attackAnimParam);
    }

    private void CacheComponents()
    {
        _controller = GetComponent<BotController>();
        _animator = GetComponent<Animator>();
    }

}
```

이제 컨트롤러 쪽에서는 핸들러만이 가진다. 그래서 BotController에서 애니메이터 관련 변수와 함수를 비활성화 시킨다. 

설정하고 실행하면 

![alt text](image-310.png)

아까와 같이 동일하게 동작함을 알 수 있다. 

### 1.10. 4방향 이동

블랜드 트리를 통한 구현 설정할 것임. 

![alt text](image-312.png)

바꿔준다.

![alt text](image-313.png)

애니메이션을 추가하고 각각의 값을 설정한다.

![alt text](image-314.png)

블랜드 트리 쪽에서 x, z축을 각각 움직여 줄 수 있음.

소스 코드는 다음과 같다. 

```csharp
using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BotController2 : MonoBehaviour
{
    public event Action<Vector2> OnMove;
    private Vector2 _prevMovement;

    private void Update() => SetMove();

    private void SetMove()
    {
        Vector2 movement = GetMovement();
        // 이전 프레임의 Movement와 같으면 return
        if (_prevMovement == movement)
            return;

        // 다르다면 OnMove + _prevMovement
        OnMove?.Invoke(movement);

        // 갱신
        _prevMovement = movement;
    }
    private Vector2 GetMovement()
    {
        // 입력받아서 Vector2 반환.=> GetAxisRaw
        // 단위벡터로 만들지 말 것. 
        return new Vector2(Input.GetAxisRaw("Horizontal"), Input.GetAxisRaw("Vertical"));
    }
}
```

```csharp
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BotAnimationHandler2 : MonoBehaviour
{
    [SerializeField] private string _moveXParam;
    [SerializeField] private string _moveZParam;

    private int _moveX;
    private int _moveZ;

    private BotController2 _controller;
    private Animator _animator;

    private void Awake()
    {
        CacheComponents();
        Init();
    }

    private void OnEnable()
    {
        BindBotEvents();
    }

    private void OnDisable()
    {
        UnbindEvents();
    }

    private void BindBotEvents()
    {
        _controller.OnMove += SetMoveAnim;
    }

    private void UnbindEvents()
    {
        _controller.OnMove -= SetMoveAnim;
    }
    
    private void SetMoveAnim(Vector2 movement)
    {
        _animator.SetFloat(_moveX, movement.x);
        _animator.SetFloat(_moveZ, movement.y);
    }

    private void Init()
    {
        _moveX = Animator.StringToHash(_moveXParam);
        _moveZ = Animator.StringToHash(_moveZParam);
    }

    private void CacheComponents()
    {
        _controller = GetComponent<BotController2>();
        _animator = GetComponent<Animator>();
    }
}
```

![alt text](image-315.png)

이런 식으로 된다. 